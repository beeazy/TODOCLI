export interface Theme {
  name: string;
  background: string;
  foreground: string;
  accent: string;
  onAccent: string;
  success: string;
  error: string;
  muted: string;
  surface: string;
  border: string;
} 